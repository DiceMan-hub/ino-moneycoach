# ino-moneycoach リポジトリルール

## 概要
GitHub Pages公開用リポジトリ（クレカ比較ページ等）

## 作業ルール
- ソース編集は `~/Projects/ino-moneycoach/` で行い、確認後に push
- `docs/` 配下が公開対象。意図しないファイルを置かない
- push前に必ずローカルでHTMLプレビュー確認
- 画像は軽量化してからコミット（不要な大容量ファイルを入れない）

## 公開中ページ
- `docs/flight-delay-insurance/` — クレカ付帯保険比較
- `docs/tax-payment/` — クレカ納税ROI比較
